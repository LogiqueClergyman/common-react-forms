// Enum to represent the valid input types
export enum FieldType {
  TEXT = 'text',
  EMAIL = 'email',
  PASSWORD = 'password',
  NUMBER = 'number',
  BOOLEAN = 'checkbox',
  RANGE = 'range',
  OPTIONS = 'options',
}

export default FieldType;
